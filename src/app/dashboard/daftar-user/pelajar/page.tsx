import DaftarUserManagement from "../_components/daftar-user";

export const metadata = {
  title: "Scodule | Daftar Pelajar Management",
  description: "Manage daftar pelajar with mock data while backend is not available.",
};

export default function DaftarPelajarManagementPage() {
  return <DaftarUserManagement />;
}
