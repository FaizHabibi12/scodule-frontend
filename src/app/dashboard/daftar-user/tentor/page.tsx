import DaftarTentorManagement from "./_components/daftar-tentor";

export const metadata = {
  title: "Scodule | Daftar Tentor Management",
  description: "Manage daftar tentor with mock data while backend is not available.",
};

export default function DaftarTentorManagementPage() {
  return <DaftarTentorManagement />;
}
