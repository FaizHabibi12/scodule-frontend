import PengaturanProfile from "./_components/pengaturan";

export const metadata = {
  title: "Scodule | Pengaturan Profile",
  description: "Manage daftar pelajar with mock data while backend is not available.",
};

export default function PengaturanPage() {
  return <PengaturanProfile/>;
}
