import Dashboard from './_components/dashboard';

export const metadata = {
    title: 'Scodule | Dashboard Admin',
    description: 'Manage application menus including creation, modification, and deletion of menu items.',
};

export default function DashboardPage() {
    return <Dashboard />;
}
